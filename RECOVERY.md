# 书架备份与章节历史（0.3.0）

书架右上角的「备份与恢复」可以创建全书架 `.novelhub` 文件，或从本机备份列表、外部文件预览并恢复。作品卡片上的「导出」会生成单部作品的 `.novelhub-project` 文件；导入作品会新增一部小说，不替换现有书架。

- 完整备份包含所有作品的正文、章节历史、设定、文风样本、主线、时间线、待办、对话、故事线、灵感画板和 AI 提案。
- 单部作品导出包含该作品的上述数据，导入时重映射全部 ID，因此可以重复导入同一文件。
- API 密钥及模型连接配置不进入备份。整库恢复保留本机模型配置。
- 整库恢复会替换当前书架。确认前显示作品数、章节数、历史版本数和备份时间。
- 恢复前必须成功保存当前书架的 `before-restore` 安全副本。校验失败、空间不足或恢复中途出错不会部分覆盖数据库。
- 恢复期间拒绝其他写入；存在运行中的写入或 AI 流式任务时拒绝开始恢复。请先结束其他窗口中的工作。
- 桌面服务每次启动检查当天快照，没有时自动创建一次。备份目录是应用数据目录下的 `backups/library/`，不会自动删除历史备份。
- 目前备份文件上限为 128 MB。请定期通过「另存」复制到其他磁盘；同磁盘备份不能防止磁盘损坏。
- 文件含正文和对话，使用 SHA-256 检查完整性，没有加密。不要公开分享私人作品的备份文件。

编辑器工具栏的「历史」在当前章节保存完成后可用。选择历史条目查看全文，再点击「将预览内容恢复为新版本」。回滚保留原有历史，若章节在预览期间发生变化则拒绝覆盖。

历史快照由 SQLite 触发器与章节写入同事务保存，覆盖手动保存、自动保存、AI 补丁、标题与分卷调整。重复保存相同内容不会产生重复快照。首次升级只能保留升级当时的正文，无法追溯升级前已被覆盖的版本。

SQLite 使用带版本号的 `schema_migrations` 表。当前架构版本为 1（章节历史）。启动时在同一事务中应用未执行的迁移；失败则整笔回滚，书架保持升级前状态。更高版本的数据库会被拒绝打开。回退已成功的架构升级请恢复升级前的书架备份。

目前这些恢复功能仅用于 SQLite 桌面存储。MySQL 开发环境先通过原有迁移工具转为 SQLite。

从 0.1 启动器升级时，请先关闭旧应用。新桌面版第一次启动且新数据目录没有数据库时，会从 `%LOCALAPPDATA%/NovelHub/novel_hub.db` 在线复制旧书架（包含 WAL 中已提交的数据）。原库保持不变；新库已存在时不会覆盖。旧版单章 TXT 备份仍留在原目录。

验证命令：`python -m unittest discover -s tests -v`。测试使用临时数据库，覆盖文件损坏、关联缺失、磁盘写入失败、恢复回滚、HTTP 流程、并发写入隔离和历史分页。
